extends ../layouts/application.pug

block header
  = t('views.tasks.new.title')

block content
  +formFor()(action=route('tasks') method='post')
    +input(task, 'name', t('views.tasks.name'), errors)(type='text')
    +textarea(task, 'description', t('views.tasks.description'))
    +columnSelect(task, 'statusId', t('views.tasks.status'), statusSelectData)
    +columnSelect(task, 'executorId', t('views.tasks.executor'), userSelectData)
    +columnSelect(task, 'labelId', t('views.tasks.label'), labelSelectData)('multiple')
    +submit(task)(value=t('views.tasks.new.button'))
